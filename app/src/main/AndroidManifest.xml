<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.android.blackoder.makta">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaktaApp"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".view.settings.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".view.MainActivity" />
        <activity
            android:name=".view.profile.WishListActivity"
            android:label="WishList"
            android:parentActivityName=".view.MainActivity" />
        <activity
            android:name=".view.profile.BookListActivity"
            android:label="My Books"
            android:launchMode="singleTop"
            android:parentActivityName=".view.MainActivity" />
        <activity
            android:name=".view.profile.BookRequestActivity"
            android:label="Book Request"
            android:parentActivityName=".view.MainActivity" />
        <activity
            android:name=".view.profile.BookEntryActivity"
            android:label="Book Entry"
            android:parentActivityName=".view.profile.BookListActivity" />
        <activity
            android:name=".view.BookDetailActivity"
            android:label="Book Details"
            android:parentActivityName=".view.profile.BookListActivity" />
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop" />
    </application>

</manifest>